package unit_change.units;

public class Data {
    public static final String[] dataUnits = { "bit", "B", "KB", "MB", "GB", "TB", "PB", "EB" };

    public static double[] calculateData(double finding, int order) {
        double[] calculated = new double[8];
        return calculated;
    }
}
